# frozen_string_literal: true

source 'https://rubygems.org'

# Postmark API client
gem 'postmark', '~> 1.25'

# Environment variables
gem 'dotenv', '~> 3.1'

# HTTP client (used by postmark gem)
gem 'net-http-persistent', '~> 4.0'

# JSON handling
gem 'json', '~> 2.7'

# YAML config
gem 'yaml', '~> 0.3'

# Rake tasks
gem 'rake', '~> 13.1'

group :development do
  # Template preview in browser
  gem 'launchy', '~> 3.0'
end
